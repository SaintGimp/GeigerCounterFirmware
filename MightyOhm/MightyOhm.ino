// We don't actually want an Arduino sketch, we're just using the
// toolchain, so we leave the .ino file empty and add an ordinary
// .c file alongside it.  Target the ATtiny2313 board from the
// ATTinyCore package, and select the ATTiny2313 chip.
